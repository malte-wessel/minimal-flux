{
  "name": "minimal-flux-todo-example",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "./node_modules/.bin/browserify src/app.js -t babelify > build.js",
    "watch": "./node_modules/.bin/watchify src/app.js -t babelify -d -o build.js"
  },
  "author": "Malte Wessel <delabreak@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "babelify": "^5.0.5",
    "browserify": "^8.1.3",
    "react": "^0.14.0-alpha2",
    "watchify": "^2.6.2"
  },
  "dependencies": {
    "react": "^0.14.0-alpha2",
    "uniqueid": "^0.1.0"
  }
}
